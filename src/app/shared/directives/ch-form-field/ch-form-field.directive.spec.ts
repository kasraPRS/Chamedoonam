import { ChFormFieldDirective } from './ch-form-field.directive';

describe('ChFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ChFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
