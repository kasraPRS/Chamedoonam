import { CaptureScrollEventDirective } from './capture-scroll-event.directive';

describe('CaptureScrollEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptureScrollEventDirective();
    expect(directive).toBeTruthy();
  });
});
