export class NavigationDTO {
    id: string;
    title: string;
    icon: string;
    url: string

    // constructor(id: string, title: string, icon: string) {
    //     this.id = id;
    //     this.title = title;
    //     this.icon = icon;
    // }
}